# 冀东装备核心备件智能预测系统

浏览器版本，对应桌面程序「冀东装备预测应用」。预测在浏览器本地完成，使用与原软件相同的 XGBoost 模型。

## 打开方式

把本仓库发布为 GitHub Pages（分支 main，目录 /）后，用地址打开即可。

本机预览：

`
python -m http.server 8765
`

然后访问 http://127.0.0.1:8765 。不要直接双击 index.html，浏览器不允许页面读取旁边的模型文件。

登录页会写明原软件的管理员和访客入口。管理员可以使用四个预测模块和两套查询；访客只能查询矿山原料和耐磨材料。
